/**
 * Seeded matrix cells — code/devtool surfaces (authored batch). Stub until
 * the batch lands; the completeness test drives this to full coverage.
 */
import type { ItemCells } from '../cells';

export const AI_CODE_CELLS: Record<string, ItemCells> = {};
