/**
 * Seeded matrix cells — UI primitives (authored batch). Stub until the batch
 * lands; the completeness test drives this to full coverage.
 */
import type { ItemCells } from '../cells';

export const UI_CELLS: Record<string, ItemCells> = {};
