/**
 * Seeded matrix cells — agent surfaces (authored batch). Stub until the
 * batch lands; the completeness test drives this to full coverage.
 */
import type { ItemCells } from '../cells';

export const AI_AGENT_CELLS: Record<string, ItemCells> = {};
