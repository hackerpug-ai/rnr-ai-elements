---
service: rnr-ai-elements
feature: journey-theming-passthrough
covers_ucs: [UC-FOUND-01, UC-FOUND-04]
priority: P0
type: security
tier: visible
test_tier: e2e
---
# One surface refuses to re-theme

Run the six-screenshot palette flip with a hex literal deliberately planted inside the markdown style object — the exact place prior art hardcoded `DARK_MARKDOWN_STYLE`. The grep gate must fail the build first. If it does not, the device pass must catch that the largest area of every assistant message did not move while all surrounding chrome did. A flip where 95% of pixels move is a FAIL, not a near-pass: the promise is that a consumer never opens a component file, and one stuck color breaks it permanently.

**Covers:** `UC-FOUND-01`, `UC-FOUND-04`
